#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: character count
 * @argv:character vector
 * Return:always zero
 */
	int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s/n", argv[0]);
	return (0);
}
