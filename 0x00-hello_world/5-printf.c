#include <stdio.h>
/**
 * main- entry point of the program
 *
 * Return: By default returns zero,  if no errors otherwise non-zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
