#include "main.h"
/**
 * _abs-it gives absolute value
 * @n: n is the integer
 * Return: positive for correct and negative otherwise
 */
	int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
